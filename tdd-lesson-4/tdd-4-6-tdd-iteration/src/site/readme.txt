 ------------------------------------------------------
 ${project.description}
 ------------------------------------------------------
 Pedro Ballesteros <pedro@theprogrammingchronicles.com>
 ------------------------------------------------------

${project.name} - ${project.description}

   Se implementa el código hace funcionar el nuevo test
   testAddDuplicateNameDifferentSurname.

   * Ejemplo de funcionalidad que rompe tests ya existentes

     La aplicación iterativa e incremental del ciclo TDD hace
     que al cumplir la funcionalidad de los nuevos tests se rompan
     test existentes.

   * Ejemplo de la seguridad y cobertura que nos proporcionan los
     tests unitarios para ir modificando código y existente, o por
     actualización de la funcionalidad o por refactorización.

   * Para corregir los tests que se han roto vemos que es necesario
     realizar nuevos tests unitarios en los que no se había pensado.